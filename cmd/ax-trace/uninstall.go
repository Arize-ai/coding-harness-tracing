package main

// Uninstall subcommands register themselves in init().
// Actual command logic is filled in by a later task.

func init() {
	// Placeholder — actual uninstall commands are registered by the
	// update-uninstall task.
}
