package main

// Doctor command registers itself in init().
// Actual command logic is filled in by a later task.

func init() {
	// Placeholder — actual doctor command is registered by the
	// diagnostic-commands task.
}
