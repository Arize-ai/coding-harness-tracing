package main

// Update subcommands register themselves in init().
// Actual command logic is filled in by a later task.

func init() {
	// Placeholder — actual update commands are registered by the
	// update-uninstall task.
}
