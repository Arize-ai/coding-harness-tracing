package main

// Config subcommands register themselves in init().
// Actual command logic is filled in by a later task.

func init() {
	// Placeholder — actual config commands are registered by a later task.
}
