package main

// Version command registers itself in init().
// Actual command logic is filled in by a later task.

func init() {
	// Placeholder — actual version command is registered by the
	// diagnostic-commands task.
}
